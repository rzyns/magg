# Docker Registry Configuration
# REGISTRY=ghcr.io/<your-github-username>

# Image source prefix for docker compose (defaults to $USER)
# SOURCE=local

# Config volume path for compose (defaults to named volume 'magg-config' except for dev)
# MAGG_CONFIG_VOLUME=./.magg

# Authentication
# MAGG_PRIVATE_KEY=  # generate with `magg auth private-key --oneline`
# MAGG_JWT=  # For clients - generate with `magg auth jwt -q`

# SSL/TLS Configuration (optional for HTTPS)
# Two modes are supported:
#
# Mode 1 (NEW - Recommended): Direct host path mounting
# Specify the full paths to your SSL certificate files on the host system
# Then use: docker compose -f compose.yaml -f compose.ssl.yaml up
# MAGG_SSL_KEYFILE_HOST=/path/to/your/private.key  # Host path to SSL key file
# MAGG_SSL_CERTFILE_HOST=/path/to/your/certificate.crt  # Host path to SSL certificate file
#
# Mode 2 (Legacy - Backward compatible): Directory mounting
# Place certificates in a directory and reference them from within the container
# Use standard: docker compose up
# MAGG_SSL_CERTS_PATH=./certs  # Host path to certificates directory (mounted as /certs in container)
# MAGG_SSL_KEYFILE=/certs/key.pem  # Path inside container to SSL key file
# MAGG_SSL_CERTFILE=/certs/cert.pem  # Path inside container to SSL certificate file
#
# Note: Mode 1 takes precedence if both are configured

# Logging
# MAGG_LOG_LEVEL=WARNING

# Other options
# MAGG_CONFIG_PATH=./magg/config.json
# MAGG_SELF_PREFIX=magg  # The prefix used in the MCP server for magg's own tools

# Config reloading

# MAGG_AUTO_RELOAD
# MAGG_RELOAD_POLL_INTERVAL
# MAGG_RELOAD_USE_WATCHDOG
# MAGG_READ_ONLY
